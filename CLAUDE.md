# Claude Adapter

Follow `AGENTS.md` and `/Users/star/.agent/CONTRACT.md`. Use the same proof gates:

```bash
./scripts/check.sh
```

Keep model credentials out of git and do not send uploaded audio off-machine without exact user authorization.
